# ... (앞부분 동일) ...
      
      - name: Generate JSON files
        run: node scripts/sync-i18n.js
        env:
          GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
          GOOGLE_CLIENT_EMAIL: ${{ secrets.GOOGLE_CLIENT_EMAIL }}
          GOOGLE_PRIVATE_KEY: ${{ secrets.GOOGLE_PRIVATE_KEY }}

      # 👇 [디버깅 강화] 전체 파일 구조를 찍어서 locales가 어디 생겼는지 확인
      - name: Debug - Find Locales
        run: |
          echo "🔍 현재 위치 파일 목록:"
          ls -F
          
          echo "🔍 혹시 public 안에 생겼나 확인:"
          ls -F public/ || echo "public 폴더 없음"
          
          echo "🔍 locales 폴더 확인:"
          ls -R locales

      # ... (Upload 단계 계속) ...